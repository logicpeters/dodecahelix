package org.dmp.gwtpurdy.client.form;

public enum LabelPosition {
    /** Label to the left of the form field, left aligned in the label column  **/
	LEFT_LEFT,
	/** Label to the left of the form field, right aligned in the label column (flush with the FormField) **/
	LEFT_RIGHT,
	TOP,
	BOTTOM,
	/** Label to the right of the form field (common with Checkbox) **/
	RIGHT
}
